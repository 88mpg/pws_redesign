<!DOCTYPE html>
<html lang="en">

<head>
  {{> global/header meta }}
</head>

<body>

<main id="main" role="main">
  {{> global/navigation }}
  {{> components/hero }}
  {{> components/quote }}
  {{> components/articles }}
  {{> components/categories }}
  {{> components/carousel }}
  {{> components/footer }}
</main>

{{> global/footer }}
</body>
</html>
